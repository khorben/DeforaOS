targets=Init
cflags_force=-W -Wall -ansi
cflags=-g
ldflags_force=-L $(PREFIX)/lib -Wl,-rpath,$(PREFIX)/lib -l System -Wl,--export-dynamic -l dl
dist=Makefile

[Init]
type=binary
sources=init.c
install=$(BINDIR)
