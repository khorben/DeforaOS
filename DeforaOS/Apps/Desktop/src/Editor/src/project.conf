targets=editor
cflags_force=-W `pkg-config --cflags gtk+-2.0`
cflags=-Wall -g -O2
ldflags_force=`pkg-config --libs gtk+-2.0`
dist=Makefile,editor.h

[editor]
type=binary
sources=editor.c,main.c
install=$(BINDIR)

[editor.c]
depends=../config.h
