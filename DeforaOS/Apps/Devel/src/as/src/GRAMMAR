as:		{ newline } { section instruction_list } { newline }

newline:	[ space ] NEWLINE

space:		SPACE { SPACE }

instruction_list:	{ function | space (instruction | newline) | newline }

section:	"." WORD newline

function:	WORD ":" newline

instruction:	operator [ space ] [ operand_list ] newline

operator:	WORD

operand_list:	operand [ space ] { "," [ space ] operand [ space ] }

operand:	WORD | NUMBER
