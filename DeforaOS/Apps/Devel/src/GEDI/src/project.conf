targets=gedi
cppflags_force=-I $(PREFIX)/include
cflags_force=-W `pkg-config --cflags gtk+-2.0`
cflags=-Wall -g -O2
ldflags_force=`pkg-config --libs gtk+-2.0` -lSystem -lDesktop
ldflags=-L $(PREFIX)/lib -Wl,-rpath,$(PREFIX)/lib
dist=Makefile,callbacks.h,gedi.h,project.h

[gedi]
type=binary
sources=callbacks.c,gedi.c,main.c,project.c
install=$(BINDIR)

[callbacks.c]
depends=../config.h,callbacks.h,gedi.h

[gedi.c]
depends=../config.h,callbacks.h,gedi.h
